import Card from "./Card";

export default function Pile() {
  // console.log(pile);

  return (
    <>
      {/* {pile.map((card) => {
        <Card {...card} hidden={true} />;
      })} */}
    </>
  );
}
