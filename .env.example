# URL do Servidor Backend (Opcional)
# Deixe sem valor (ou comentado) ao rodar local via "bun run host" ou túnel ngrok na mesma porta, 
# pois o sistema detectará automaticamente a origem do servidor!
# Preencha apenas caso vá hospedar a interface gráfica (Frontend) separada do Backend na nuvem.
# VITE_SERVER_URL=http://localhost:3050
